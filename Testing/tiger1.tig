/* an array type and an array variable */
let
	var a : int := 1
in
	1-2;
	3;
	a;
	a := 1;
	b := (a<>1;a>0;a-1;2+a*b-2/c;a|2);
	if a then b;
	if a>0 then b:=b+1;
	if aa>0 then bb:=bb+1 else bb:=bb-1
end