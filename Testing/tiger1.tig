let
	type i = array of int
	type any = {a : int, i : any}
	function try(c:int,y:string) =
	a := 1
	type my = {any : string, i : string, aa : string}
	var a := 1 + 2
	var b := 4
in
    1+2;
    if a then b;
    if aa>0 then bb:=bb+1 else bb:=bb-1;
    if a then b;
    if a & b then a := b+1;
	if aa>0 then bb:=bb+1 else bb:=bb-1;
    while i<10 do i:= i * 2;
    for i := 1 to 2 do a := a*2;

    1;
    1+2;
    "100";
    a := nil;
    f(a);
    (a-1; a+2; a-4);
	a;
	a := 1;
	b := (a<>1;a>0;a-1;2+a*b-2/c;a|2);
	print(f(a,g(b)))
end